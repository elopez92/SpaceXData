<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SpaceXData" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/blue</item>
    </style>

    <style name="Theme.SpaceXData.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SpaceXData.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SpaceXData.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>